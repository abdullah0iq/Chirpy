version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        out: "internal/database"
        emit_json_tags: true # Add JSON tags to structs
        json_tags_case_style: "snake" # Use snake_case for JSON tags
